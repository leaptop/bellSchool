Если элемент исчез со страницы, то выдаётся ошибка stale element

Изучать: XPath, Java Streams, lambdas

Pagefactory работает быстрее, но минус в том, что в findby нельзя прописывать составные строки с участием переменных ( "//div[] + category + "..." - такое не прокатит. Нужны цельные строки).


